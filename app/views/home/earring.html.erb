<h1>Earrings</h1>

<div class="center">
  <label for="sort_by">Sort by:</label>
    <%= select_tag 'earring_sort_by', options_for_select([['All', 'all']] + @subtypes.map { |subtype| [subtype, subtype] }),
    prompt: 'Subtype',
    id: 'earring_sort_by',
    data: { 
      controller: "subtypeSort",
      action: "subtypeSort#update"
     } %>
</div>

<%= render 'home/sorted' %>