<p class='center underline fs-3'>Profile Bio:</p>

<%= form_with(model: @user, url: profile_update_path(@user), method: :patch) do |f| %>
  <div class='container center'>
    <%= f.text_area :bio, rows: 4 %>
  </div>
  <div class='center'>
    <%= f.submit 'Update Bio' %>
  </div>
<% end %>